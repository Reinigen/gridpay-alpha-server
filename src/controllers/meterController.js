import MeterModel from "../models/meterModel.js";

class MeterController {
  static addMeter = async (req, res, next) => {
    const { meterId, address } = req.body;
    try {
        const newMeter = 
    } catch (error) {
        
    }
  };
}

export default MeterController;
